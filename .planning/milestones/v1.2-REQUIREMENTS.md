# Requirements: v1.2 Upstream Resilience (archived)

**Shipped:** 2026-04-21 (final polish 2026-04-24)
**Outcome:** 9/9 requirements satisfied. No deferrals, no scope drift from kickoff to ship.

## Requirements at ship

### File-Layout Drift Detection
- [x] **DRIFT-01**: Automated detector scans all `@~/.claude/...` and `@$HOME/.claude/...` references in plugin content; flags any reference pointing at a file that doesn't exist in the plugin layout — Phase 7 (`bin/maintenance/check-file-layout.cjs`; commits `63444dd` + `777def6`)
- [x] **DRIFT-02**: Drift detectors (file-layout, schema, namespace) run in CI and hard-fail on any detected drift — Phases 7 (file-layout CI job `9450005` + skip-list fix `777def6`) + 8 (schema CI job `fdcab58`) + 9 (namespace detection via umbrella `0170c3f`, `rewrite-command-namespace.cjs --dry` exit-0 checked in the consolidated report)

### HANDOFF Schema Baseline and Drift Detection
- [x] **SCHEMA-01**: Committed `schema/handoff-v1.json` describes the 19-field HANDOFF.json contract — 17 required upstream-compat fields + 2 optional plugin-only fields (`source`, `partial`); `additionalProperties: true` for forward compat — Phase 8 (commit `1626112`)
- [x] **SCHEMA-02**: `writeCheckpoint()` output validates against `handoff-v1.json` in CI; schema violations hard-fail — Phase 8 (`bin/maintenance/check-handoff-schema.cjs`; commits `f18d357` + `fdcab58`)
- [x] **SCHEMA-03**: Post-upstream-sync check compares upstream's `/gsd:pause-work` output structure against the plugin's HANDOFF schema — Phase 8 (`bin/maintenance/check-upstream-schema.cjs`; commit `3d67575`). Confirmed PASS against upstream 1.38.3 at ship time.

### Unified Maintenance + Docs
- [x] **DRIFT-03**: Single `bin/maintenance/check-drift.cjs` entry-point runs file-layout + schema + namespace detectors in one invocation and reports a consolidated PASS/FAIL — Phase 9 (commit `0170c3f`)
- [x] **DOCS-01** (carried from v1.1): Full README paragraph documenting session continuity feature + drift-resilience story — Phase 9 (commit `7fd66c8`). Partially pre-satisfied by v1.1's quick task `260420-rar` (auto-resume bullet); Phase 9 added the full two-paragraph `## Session continuity + drift resilience` section.
- [x] **DOCS-02** (carried from v1.1): `CHANGELOG.md` scaffold tracking plugin vs upstream versions side-by-side, starting with v2.38.2 (v1.1 shipped) and working forward — Phase 9 (commit `34a348c`). Keep-a-Changelog format with plugin-vs-upstream version distinction in section headers.
- [x] **MAINT-01**: PROJECT.md "After each upstream GSD sync" checklist formalized — 9-step sequence including mandatory `check-drift.cjs` gate and separate `check-upstream-schema.cjs` step — Phase 9 (commit `f9561e7`)

## Reframed from v1.1 backlog

| v1.1 Req | Fate in v1.2 | Outcome |
|----------|--------------|---------|
| UPST-01 | **Reframed** → **SCHEMA-03** at v1.2 kickoff. Goal shifted from "make HANDOFF compatible for upstream PR submission" to "detect drift between upstream's `pause-work` output and our schema." | **SATISFIED** as SCHEMA-03 (Phase 8). Research R-1 confirmed upstream schema is stable across 1.37.x → 1.38.x and plugin is a strict superset; drift-detection-baseline framing is the right one. |

## Research outcomes

- [x] **R-1**: What does upstream GSD's `/gsd:pause-work` actually produce? Is the output structure stable across 1.34 → 1.38.x versions? **Resolved at Phase 8 plan time (2026-04-21):** upstream produces `.planning/HANDOFF.json` with 17 fields; schema is stable across 1.37.x → 1.38.x (byte-identical workflow body in inspected versions); plugin is a strict superset (upstream's 17 + plugin-only `source` + `partial`). Full findings at `v1.2-phases/08-handoff-schema-detector/08-RESEARCH.md`.

## Still deferred (to v1.3+)

Carried forward from v1.1 and kept deferred:

### Drift detection — behavior category
- **BEHAVIOR-01** *(future)*: Integration tests detect semantic regressions where an upstream skill keeps the same name but changes behavior. Needs integration-test infrastructure which v1.2 does not build.

### Checkpoint lifecycle polish
- **LIFE-02** *(future)*: Stale HANDOFF.json (older than configurable threshold) detection. Nice-to-have polish; not drift-related.
- **LIFE-03** *(future)*: Dedicated `/gsd:checkpoint` skill for manual save. Manual path already covered by `/gsd:pause-work` + `checkpoint --source manual-pause`.

### Upstream-PR track
- **UPST-03** *(future)*: Plugin-side changes packaged as isolated upstream-ready patches. Blocked on reassessment after v1.2 drift-detection baseline informs whether upstream is still the right destination.
- **UPST-04** *(future)*: Patch files / PR-ready diff prepared for upstream submission. Blocked on UPST-03.

## Out of Scope

- **Auto-patching on drift** — detectors report, humans fix. Auto-patching is tempting for namespace drift (the rewrite script could self-trigger) but adds complexity and hides what's actually changing. Keep detection separate from mitigation.
- **Multi-version matrix testing** — we don't spin up test environments with each upstream version to check for behavior drift.
- **Upstream contribution workflow beyond detection** — we detect when upstream drifts from us; we do not automate the decision of whether to chase the drift (compat patch) or let it diverge. Human judgment call per incident.
- **Protecting against Claude Code drift** — scope is upstream GSD, not upstream CC. CC's `CLAUDE_PLUGIN_ROOT` behavior is orthogonal and mitigated in v1.1 (260420-vfb).

## Traceability (final)

| Requirement | Phase | Final status | Evidence |
|-------------|-------|--------------|----------|
| DRIFT-01 | 7 | Satisfied | `check-file-layout.cjs` (63444dd) + skip-list fix (777def6) |
| DRIFT-02 | 7, 8, 9 | Satisfied | CI: file-layout job (9450005), schema job (fdcab58); umbrella parses namespace --dry (0170c3f) |
| SCHEMA-01 | 8 | Satisfied | `schema/handoff-v1.json` (1626112) |
| SCHEMA-02 | 8 | Satisfied | `check-handoff-schema.cjs` (f18d357) + CI wiring (fdcab58) |
| SCHEMA-03 | 8 | Satisfied | `check-upstream-schema.cjs` (3d67575); R-1 resolved at plan time |
| DRIFT-03 | 9 | Satisfied | `check-drift.cjs` umbrella (0170c3f) |
| DOCS-01 | 9 | Satisfied | README `## Session continuity + drift resilience` (7fd66c8) |
| DOCS-02 | 9 | Satisfied | `CHANGELOG.md` scaffold (34a348c) |
| MAINT-01 | 9 | Satisfied | PROJECT.md 9-step post-sync checklist (f9561e7) |
| BEHAVIOR-01 | — | Deferred to v1.3+ | Needs integration-test infra |
| LIFE-02 | — | Deferred to v1.3+ | Polish, not drift-related |
| LIFE-03 | — | Deferred to v1.3+ | Manual path already covered |
| UPST-03 | — | Deferred to v1.3+ | Blocked on upstream-direction review |
| UPST-04 | — | Deferred to v1.3+ | Blocked on UPST-03 |
